name: Deploy content

on:
  push:
    branches:
      - dev
      - prod
    paths:
      - 'sites/**'
      - '.github/workflows/deploy-content.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: Entorno a desplegar
        required: true
        type: choice
        options:
          - dev
          - prod

permissions:
  contents: read

env:
  AWS_REGION: us-east-1

jobs:
  deploy:
    name: Sync S3 + CloudFront
    runs-on: ubuntu-latest
    environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || github.ref_name }}
    steps:
      - uses: actions/checkout@v4

      - name: Validar credenciales AWS
        env:
          KEY_FROM_SECRET: ${{ secrets.AWS_ACCESS_KEY_ID }}
          KEY_FROM_VAR: ${{ vars.AWS_ACCESS_KEY_ID }}
          SECRET_FROM_SECRET: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          SECRET_FROM_VAR: ${{ vars.AWS_SECRET_ACCESS_KEY }}
        run: |
          AWS_ACCESS_KEY_ID="${KEY_FROM_SECRET:-$KEY_FROM_VAR}"
          AWS_SECRET_ACCESS_KEY="${SECRET_FROM_SECRET:-$SECRET_FROM_VAR}"
          if [ -z "$AWS_ACCESS_KEY_ID" ] || [ -z "$AWS_SECRET_ACCESS_KEY" ]; then
            echo "::error::Faltan AWS_ACCESS_KEY_ID y/o AWS_SECRET_ACCESS_KEY."
            echo "::error::Configúralos en Settings → Secrets and variables → Actions (Variables o Secrets)."
            exit 1
          fi

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID != '' && secrets.AWS_ACCESS_KEY_ID || vars.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY != '' && secrets.AWS_SECRET_ACCESS_KEY || vars.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verificar cuenta AWS
        run: |
          echo "Región: ${AWS_REGION}"
          aws sts get-caller-identity --output table

      - name: Resolver bucket y CloudFront
        id: target
        env:
          ENV_NAME: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || github.ref_name }}
          S3_GENERIC_VAR: ${{ vars.S3_BUCKET }}
          S3_GENERIC_SECRET: ${{ secrets.S3_BUCKET }}
          CF_GENERIC_VAR: ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}
          CF_GENERIC_SECRET: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}
          DEV_S3_VAR: ${{ vars.DEV_S3_BUCKET }}
          DEV_S3_SECRET: ${{ secrets.DEV_S3_BUCKET }}
          DEV_CF_VAR: ${{ vars.DEV_CLOUDFRONT_DISTRIBUTION_ID }}
          DEV_CF_SECRET: ${{ secrets.DEV_CLOUDFRONT_DISTRIBUTION_ID }}
          PROD_S3_VAR: ${{ vars.PROD_S3_BUCKET }}
          PROD_S3_SECRET: ${{ secrets.PROD_S3_BUCKET }}
          PROD_CF_VAR: ${{ vars.PROD_CLOUDFRONT_DISTRIBUTION_ID }}
          PROD_CF_SECRET: ${{ secrets.PROD_CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          ENV_NAME="${{ github.event_name == 'workflow_dispatch' && inputs.environment || github.ref_name }}"
          STACK_NAME="mastersaws-portfolio-${ENV_NAME}"
          S3_BUCKET=""
          CF_ID=""

          # 1. IaC primero: leer bucket y CloudFront del stack CloudFormation
          if aws cloudformation describe-stacks --stack-name "$STACK_NAME" --region "$AWS_REGION" >/dev/null 2>&1; then
            echo "Stack ${STACK_NAME} encontrado. Leyendo outputs de IaC..."
            OUTPUTS=$(aws cloudformation describe-stacks \
              --stack-name "$STACK_NAME" \
              --region "$AWS_REGION" \
              --query 'Stacks[0].Outputs' \
              --output json)
            S3_BUCKET=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="BucketName") | .OutputValue')
            CF_ID=$(echo "$OUTPUTS" | jq -r '.[] | select(.OutputKey=="CloudFrontDistributionId") | .OutputValue')
          else
            echo "Stack ${STACK_NAME} no existe. Buscando variables manuales (opcional)..."
            case "$ENV_NAME" in
              dev)
                S3_BUCKET="${DEV_S3_VAR:-$DEV_S3_SECRET}"
                CF_ID="${DEV_CF_VAR:-$DEV_CF_SECRET}"
                ;;
              prod)
                S3_BUCKET="${PROD_S3_VAR:-$PROD_S3_SECRET}"
                CF_ID="${PROD_CF_VAR:-$PROD_CF_SECRET}"
                ;;
            esac
            S3_BUCKET="${S3_BUCKET:-${S3_GENERIC_VAR:-$S3_GENERIC_SECRET}}"
            CF_ID="${CF_ID:-${CF_GENERIC_VAR:-$CF_GENERIC_SECRET}}"
          fi

          if [ -z "$S3_BUCKET" ] || [ "$S3_BUCKET" = "null" ]; then
            echo "::error::No hay infraestructura desplegada."
            echo "::error::Ejecuta: Actions → Deploy all → Run workflow → ${ENV_NAME}"
            echo "::error::Eso crea el bucket S3 y CloudFront por IaC. No necesitas configurar nombres manualmente."
            exit 1
          fi
          if [ -z "$CF_ID" ] || [ "$CF_ID" = "null" ]; then
            echo "::error::No se encontró CloudFront. Ejecuta Deploy all → ${ENV_NAME}"
            exit 1
          fi

          echo "s3_bucket=$S3_BUCKET" >> "$GITHUB_OUTPUT"
          echo "cloudfront_id=$CF_ID" >> "$GITHUB_OUTPUT"
          echo "Entorno: $ENV_NAME"
          echo "Bucket: $S3_BUCKET"
          echo "CloudFront: $CF_ID"

      - name: Sincronizar sitios a S3
        run: |
          aws s3 sync sites/ "s3://${{ steps.target.outputs.s3_bucket }}/" \
            --delete \
            --exact-timestamps

      - name: Invalidar CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id "${{ steps.target.outputs.cloudfront_id }}" \
            --paths "/*"
